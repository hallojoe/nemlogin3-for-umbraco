name: CI Build Validation

on:
  push:
    branches:
      - develop
      - main
  pull_request:
    branches:
      - develop
      - main

permissions:
  contents: read

env:
  DOTNET_VERSION: 10.0.x
  PACKAGE_PROJECT: src/Casko.NemLogin3ForUmbraco/Casko.NemLogin3ForUmbraco.csproj

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore package project
        run: dotnet restore "${{ env.PACKAGE_PROJECT }}"

      - name: Build package project
        run: dotnet build "${{ env.PACKAGE_PROJECT }}" --configuration Release --no-restore

      - name: Pack package project
        run: dotnet pack "${{ env.PACKAGE_PROJECT }}" --configuration Release --no-build -o ./artifacts
